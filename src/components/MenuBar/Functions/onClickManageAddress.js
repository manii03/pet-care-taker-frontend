import { MANAGE_ADDRESS } from "../../../constants/common";
import NavigateToPage from "../../../utils/navigationUtils";

function onClickManageAddress(navigate) {
  NavigateToPage(navigate, MANAGE_ADDRESS);
}

export default onClickManageAddress;
