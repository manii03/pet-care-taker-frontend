import { HOME } from "../../../constants/common";
import NavigateToPage from "../../../utils/navigationUtils";

function onClickContact(navigate) {
  NavigateToPage(navigate, HOME);
}

export default onClickContact;
